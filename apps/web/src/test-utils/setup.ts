/**
 * 브라우저 테스트 준비.
 *
 * ⛔ **실제 스타일을 불러온다.** 테스트는 진짜 Chrome에서 도는데도 CSS가
 *    하나도 적용되지 않고 있었다. 그래서 각주 버튼이 `line-height: 0`으로
 *    무너져 **실제로 눌리지 않는 상태**였는데도 테스트는 전부 통과했다 —
 *    DOM에 있기만 하면 됐기 때문이다.
 *
 *    스타일을 불러오면 크기·가시성·겹침 같은 것을 테스트가 실제로 볼 수 있다.
 */
import '../styles/index.css'
